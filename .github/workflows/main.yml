name: Hello Kakoly Workflow

on:
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello Kakoly! Your workflow is running perfectly 🎉"
